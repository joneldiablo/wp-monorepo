'use strict';

import FormSize from '../form-size';

describe('FormSize View', function() {

  beforeEach(() => {
    this.formSize = new FormSize();
  });

  it('Should run a few assertions', () => {
    expect(this.formSize);
  });

});
