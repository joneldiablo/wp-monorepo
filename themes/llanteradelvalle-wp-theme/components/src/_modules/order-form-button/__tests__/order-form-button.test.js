'use strict';

import OrderFormButton from '../order-form-button';

describe('OrderFormButton View', function() {

  beforeEach(() => {
    this.orderFormButton = new OrderFormButton();
  });

  it('Should run a few assertions', () => {
    expect(this.orderFormButton);
  });

});
